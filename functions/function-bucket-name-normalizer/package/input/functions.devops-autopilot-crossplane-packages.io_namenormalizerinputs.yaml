---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: namenormalizerinputs.functions.devops-autopilot-crossplane-packages.io
spec:
  group: functions.devops-autopilot-crossplane-packages.io
  names:
    categories:
    - crossplane
    kind: NameNormalizerInput
    listKind: NameNormalizerInputList
    plural: namenormalizerinputs
    singular: namenormalizerinput
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          maxLength:
            description: MaxLength is the maximum allowed length for the normalized
              name.
            minimum: 1
            type: integer
          metadata:
            type: object
          provider:
            type: string
          sourceFieldPath:
            type: string
          targetFieldPath:
            type: string
        required:
        - maxLength
        - provider
        - sourceFieldPath
        - targetFieldPath
        type: object
    served: true
    storage: true
